export type ContactProps = {
  id: string;
  firstName: string;
  surName: string;
  phone: string;
  email: string;
  notes: string;
};

export type NewContact = {
  firstName: string;
  surName: string;
  phone: string;
  email: string;
  notes: string;
};
